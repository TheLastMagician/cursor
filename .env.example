# Anthropic API Key (required for real agent mode)
# Without this key, the system runs in mock/demo mode
ANTHROPIC_API_KEY=sk-ant-xxxxx

# Agent workspace directory (where agent executes tasks)
AGENT_WORKSPACE=/tmp/agent-workspace

# Server port
PORT=3001
